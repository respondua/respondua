name: Build and Deploy Docs

on:
  push:
    branches: [main]
    paths:
      - 'docs/**'  

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        working-directory: docs
        with:
          ruby-version: '3.1'
          bundler-cache: true

      - name: Install Dependencies
        working-directory: docs
        run: |
          gem install bundler
          bundle install

      - name: Build site
        working-directory: docs
        run: bundle exec jekyll build --source _docs --destination docs

      - name: Push to gh-pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: docs/_site
